apiVersion: v1
kind: Pod
metadata:
  name: wordpress-pod
  labels:
    app: wordpress
  namespace: wp
spec:
  containers:
  - name: wp-cotr
    image: wordpress
    ports:
    - containerPort: 80
    envFrom:
    - configMapRef:
        name: wp-configmap
    - secretRef:
        name: wp-secret
